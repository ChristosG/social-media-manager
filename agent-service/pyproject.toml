[project]
name = "agent-service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.115.6",
  "uvicorn[standard]==0.34.0",
  "pydantic==2.10.4",
  "pydantic-settings==2.7.1",
  "asyncpg==0.30.0",
  "pyjwt==2.10.1",
  "cryptography==44.0.1",
  "langgraph==0.2.62",
  "langchain-core==0.3.63",   # bumped from 0.3.28 to satisfy the langfuse callback's langchain dep
  "langchain-openai==0.2.14",
  "httpx==0.28.1",
  "redis==5.2.1",   # streaming-resume buffer (survives restart / past the in-memory grace window)
  "python-multipart==0.0.20",
  "pypdf==5.1.0",
  "trafilatura==2.0.0",
  "feedparser==6.0.11",
  "pillow>=11.0.0",
  "langchain==0.3.13",   # required by the langfuse langchain callback handler
  "langfuse>=2.53,<3",   # optional observability; tracing no-ops when keys are unset
]

[project.optional-dependencies]
dev = ["pytest==8.3.4", "pytest-asyncio==0.25.0"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
